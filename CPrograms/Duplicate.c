#include <stdio.h>

int main(){

        int num[6]={23,54,12,45,23,64};
        printf("%d %d %d %d %d %d",num[0],num[1],num[2],num[3],num[4],num[5]);

        int num[10];

        for (int i = 0; i < 10; i++)
        {
            printf("Enter the number: ");
            scanf("%d", &num[i]);
        }

        for (int i = 0; i < 10; i++)
        {
            printf("%d\t", num[i]);
        }
        if (num[6]=)
        {

            printf("%d",num[6]);
        }
        else{

            printf("%d",num[0]);
        }
        
return 0;
}